UPDATE prompts
SET content = '# EventKit JSON 提取提示词

请严格按照以下要求，将非结构化文本转换为格式良好的JSON格式化字符串。
用于 iOS/macOS EventKit，创建日历/事件/待办提醒，所需要的 EKEvent 类字段数据。
文本可能包含活动、事件、读书会、直播预告、线下活动预告等不同形式。**此外，您将提供当前用户日历系统中已定义的日历名称列表。
请在分析文本后，为每个事件选择最符合的日历名称标签，并将其填充在 `calendar` 字段中。**请按照以下字段定义进行提取，并确保JSON结构的准确性和正确格式化。
如果某些字段缺失或不明确，请在remarks中注明。

**日历名称列表：**
[CALENDAR_NAMES_LIST]

**用户上下文：**
[USER_CONTEXT]

**字段定义：**

1. **title**（活动名称/标题）
2. **location**（活动地点）
3. **notes**（活动备注/说明）
4. **startDate**（活动开始时间，格式为 `yyyy-MM-dd HH:mm:ss`）
5. **endDate**（活动结束时间，格式为 `yyyy-MM-dd HH:mm:ss`）
6. **url**（活动链接，通常为网页或海报）
7. **calendar**（活动所属日历，从提供的日历名称列表中选择最合适的，并填充在此字段中）
8. **status**（活动状态，如是否已发布）
9. **eventIdentifier**（活动唯一标识符，请生成一个 UUID）
10. **remarks**（如有不确定的信息，请在此注明）

**JSON 模板：**
{
  "title": "值",
  "location": "值",
  "notes": "值",
  "startDate": "值",
  "endDate": "值",
  "url": "值",
  "calendar": "值",
  "status": "值",
  "eventIdentifier": "直接生成的UUID",
  "remarks": "如有不确定的信息，请在此注明"
}

**要求：**

1. **字段提取**：从文本中提取上述所有字段的信息。如果某个字段在文本中不存在或无法确定，字段可填空。
2. **UUID 生成**：为每个事件生成一个唯一的 `eventIdentifier`（UUID 格式），可以直接生成后填充。
3. **数据格式**：
   - **日期时间**：确保 `startDate` 和 `endDate` 使用格式：yyyy-MM-dd HH:mm:ss
   - **URL**：验证 `url` 字段是否为有效的链接格式。
4. **处理不确定信息**：对于缺失或模糊的信息，尽量推断并在“remarks”中注明。例如，“开始时间不明确，推测为2024年5月”。
5. **字段一致性**：确保所有字段名准确无误，遵循大小写和拼写规范。
6. **多语言支持**：提示词和输出内容使用一致的语言，避免语言混杂导致解析错误。
7. **处理复杂文本**：
   - **特殊字符和标点**：删除不影响信息表达的 Emoji 和特殊字符，保留必要的标点符号，确保提取的信息清晰准确。
   - **中英文混杂**：优先提取中文信息，如中文信息缺失，再提取英文信息。
   - **段落不一致**：能够应对文本中段落不一致的情况，准确提取所需信息。
8. **输出格式**：严格要求使用标准的，纯净的 JSON 字符串，不要混杂任何其他任何格式。
9. **输出不要使用 markdown 格式，不要使用任何 markdown 语法。**
10. **稳定输出**：确保无论输入文本的复杂度如何，都能生成符合上述格式且能被正常解析的 JSON。
11. 如果未识别到结束时间，默认结束时间为开始时间后 2 小时。
12. 如果文中未明确说明年份，请使用【用户上下文】中指定的年份。
13. 总结汇总文本内容（summary），字数不超过 100 字，填充到 `notes` 字段中。

**示例输出JSON**
{
  "title": "年度会议",
  "location": "上海会议中心",
  "notes": "讨论公司年度业绩及未来规划。",
  "startDate": "2024-05-20 09:00:00",
  "endDate": "2024-05-20 17:00:00",
  "url": "<https://www.example.com/meeting>",
  "calendar": "公司日历",
  "status": "已发布",
  "eventIdentifier": "123e4567-e89b-12d3-a456-426614174000",
  "remarks": "无"
}

**请根据上述要求处理以下文本：**
[PLACEHOLDER_TEXT]
',
    version = version + 1,
    updated_at = NOW()
WHERE id = '444182e7-c157-4ff8-a6fa-6a87063e7c9f';