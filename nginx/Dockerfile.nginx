FROM nginx:alpine

COPY nginx.conf /etc/nginx/conf.d/default.conf

# 创建SSL证书目录并安装curl用于健康检查
RUN mkdir -p /etc/nginx/ssl && \
    apk add --no-cache curl

EXPOSE 80 443

CMD ["nginx", "-g", "daemon off;"] 